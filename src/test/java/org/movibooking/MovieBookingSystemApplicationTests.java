package org.movibooking;

import org.junit.jupiter.api.Test;
import org.moviebooking.MovieBookingSystemApplication;
import org.springframework.boot.test.context.SpringBootTest;

/* @SpringBootTest(classes = MovieBookingSystemApplication.class)*/
class MovieBookingSystemApplicationTests {

    @Test
    void contextLoads() {
        // This test ensures that the Spring application context loads successfully
        // If the application context loads without any exceptions, the test passes
    }
}
